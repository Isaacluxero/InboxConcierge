{
  "name": "inbox-concierge",
  "version": "1.0.0",
  "description": "AI-powered email management with smart search",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build",
    "dev:local": "./start.sh",
    "stop": "./stop.sh",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "setup": "npm run install:all && cd backend && npx prisma generate && npx prisma migrate dev --name init",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "logs:backend": "tail -f .logs/backend.log",
    "logs:frontend": "tail -f .logs/frontend.log"
  },
  "keywords": [
    "email",
    "ai",
    "claude",
    "gmail",
    "search"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
